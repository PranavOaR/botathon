'use client';

/**
 * Deprecated. Reasoning trace is now folded into `InvestigationCard`,
 * which renders steps inline within an assistant chat card.
 *
 * Kept as a thin re-export for backward compatibility.
 */
export { default } from './InvestigationCard';
