'use client';

/**
 * Deprecated. The left rail has been removed in the chatbot-first redesign.
 * Integration status now lives in `IntegrationsDrawer` accessible from the topbar.
 *
 * This stub remains only to avoid breaking any stale imports during migration.
 * It renders nothing.
 */
export default function LeftRail() {
  return null;
}
