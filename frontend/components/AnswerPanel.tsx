'use client';

/**
 * Deprecated. The right answer panel was removed in the chatbot-first redesign.
 * Answers now render inline as an `AnswerCard` in the chat feed.
 */
export { default } from './AnswerCard';
